<?xml version="1.0" encoding="utf-8"?>
<!-- Vista colapsada: nombre/categoría + cronómetro + un par de botones chicos (solo
     ícono), para poder pausar/parar sin necesidad de expandir. Sin swatch de color acá
     (a diferencia de la expandida): a este ancho, el nombre necesita el espacio más que
     la identidad visual — el swatch sigue estando en la vista expandida. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingLeft="12dp"
    android:paddingRight="12dp"
    android:paddingTop="8dp"
    android:paddingBottom="8dp">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/n_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAppearance="@style/TextAppearance.Compat.Notification.Title"
            android:textSize="13.5sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:text="Actividad" />

        <TextView
            android:id="@+id/n_cat"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAppearance="@style/TextAppearance.Compat.Notification"
            android:textSize="10sp"
            android:letterSpacing="0.05"
            android:textAllCaps="true"
            android:maxLines="1"
            android:ellipsize="end"
            android:text="Categoría" />
    </LinearLayout>

    <Chronometer
        android:id="@+id/n_chrono"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="@style/TextAppearance.Compat.Notification.Title"
        android:textSize="19sp"
        android:fontFamily="monospace"
        android:layout_marginStart="6dp"
        android:text="0:00" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginStart="6dp">

        <FrameLayout
            android:id="@+id/n_pause"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:background="@drawable/circle_primary"
            android:clickable="true"
            android:focusable="true">

            <ImageView
                android:id="@+id/n_pause_icon"
                android:layout_width="13dp"
                android:layout_height="13dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_pause" />
        </FrameLayout>

        <FrameLayout
            android:id="@+id/n_stop"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginStart="6dp"
            android:background="@drawable/circle_ghost"
            android:clickable="true"
            android:focusable="true">

            <ImageView
                android:id="@+id/n_stop_icon"
                android:layout_width="11dp"
                android:layout_height="11dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_stop" />
        </FrameLayout>
    </LinearLayout>
</LinearLayout>
